MODULE main
	int n;
	int k;
	int i;
	int j;

	assume(j == 0);
	assume(n >= 0);
	assume(i == 0);
	assume(k == 1 || k >= 0);

	while(i <= n){
		i = i + 1;
		j = j + i;
	}

	assert(k + i + j > 2 * n);
	

END MODULE
