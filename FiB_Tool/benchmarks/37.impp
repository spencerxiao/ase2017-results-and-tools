MODULE main
	int x;
	int m;
	int n;

	assume(x == 0);
	assume(m == 0);
	assume(n > 0);

	while(x < n){
		if(*){
			m = x;		
		}
		else{
			skip;
		}

		x = x + 1;
	}

	assert((n <= 0) || (0 <= m && m < n));	

END MODULE
